dev:
	docker compose up -d
	@echo "Stack running. Airflow UI: http://localhost:8080"

down:
	docker compose down

logs:
	docker compose logs -f

test:
	cd backend && python -m pytest tests/ -v

ingest:
	docker compose exec celery-worker python -m app.etl.ingest

shell:
	cd backend && python